//let x;
//for (x=3; x <= 36; x+=3){
    //console.log("value of x :" + x);
//}
//let y;
//for (y=4; y <= 48; y+=4){
    //console.log("value of y:" + y );
//}
//let f;
//for (f=5; f <= 40; f+=5){
    //console.log("value of f:" + f);
//}
//let G;
//for (G=6; G <= 56; G+=6){
    //console.log("value of 6:" + G); 
//}

for (let w = 1; w <= 12; w++)  {
    const result = 3 * w;
    console.log("3 * " + w + "=" + result);
}

for (let s = 1; s <= 12; s++)  {
    const output = 4 * s;
    console.log("4 * " + s + "=" + output);
}

for (let u = 1; u <= 12; u++)  {
    const Result = 5 * u;
    console.log("5 * " + u + "=" + Result);
}

for (let h = 1; h <= 12; h++)  {
    const Output = 6 * h;
    console.log("6 * " + h + "=" + Output);
}

for (let k = 1; k <= 12; k++)  {
    const rt = 7 * k;
    console.log("7 * " + k + "=" + rt);
}

for (let l = 1; l <= 12; l++)  {
    const ly = 8 * l;
    console.log("8 * " + l + "=" + ly);
}

for (let x = 1; x <= 12; x++)  {
    const xx = 9 * x;
    console.log("9 * " + x + "=" + xx);
}

for (let o = 1; o <=12; o++)  {
    const op = 10 * o;
    console.log("10 * " + o + "=" + op);
}
